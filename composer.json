{
    "name": "sas/mini-calculatrice",
    "description": "Mini-calculatrice CLI en PHP pour opérations logiques binaires",
    "require": {
        "php": ">=8.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "build": "composer install --no-dev --optimize-autoloader",
        "validate": "composer validate",
        "save": "php bin/calc.php (Get-Content samples/input.txt) --json | Set-Content samples/output.json"
    }
}